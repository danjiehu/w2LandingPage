<block wx:for="{{stories}}" wx:for-item="story">
  <view class="card-product">
    <view class="card-product-infos" data-id="{{story.id}}" bindtap="showStory">
      <view class="h2">{{story.content}}</view>
      <view class="p">{{story.name}}</view>
    </view>
  </view>
</block>